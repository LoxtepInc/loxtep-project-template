{
  "$schema": "https://loxtep.com/schemas/blueprint/pipeline.json",
  "blueprint_type": "pipeline",
  "slug": "orders-ingestion",
  "name": "Orders Webhook Ingestion",
  "description": "Ingest orders from external store via webhook into the Raw Orders data product.",
  "version": "1.0.0",
  "tags": ["webhook", "orders", "ingestion", "e-commerce"],
  "entity": {
    "pipeline_id": "{{PIPELINE_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "pipeline_type": "ingestion",
    "upstream_flow_id": null,
    "name": "{{PIPELINE_NAME}}",
    "description": "Webhook ingestion pipeline for order events",
    "template_id": "{{TEMPLATE_ID}}",
    "connection_id": "{{CONNECTION_ID}}",
    "configuration": { "schedule": null, "batch_size": 100 },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": { "blueprint_slug": "orders-ingestion", "ingestion_type": "webhook" },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  }
}
