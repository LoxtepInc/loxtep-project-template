{
  "connection_id": "{{CONNECTION_ID}}",
  "pipeline_id": "{{PIPELINE_ID}}",
  "organization_id": "{{ORGANIZATION_ID}}",
  "project_id": "{{PROJECT_ID}}",
  "key": "consumption-webhook-{{CONNECTION_ID}}",
  "name": "Webhook delivery",
  "description": "Deliver events to subscriber webhook URLs",
  "type": "webhook",
  "status": "active",
  "upstream_entity_id": "{{DATA_PRODUCT_ID}}",
  "upstream_entity_type": "data-products",
  "configuration": {},
  "metadata": { "blueprint_slug": "consumption-webhook" },
  "created_at": "{{CREATED_AT}}",
  "updated_at": "{{UPDATED_AT}}"
}
