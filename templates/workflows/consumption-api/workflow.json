{
  "$schema": "https://loxtep.io/schemas/template/workflow.json",
  "template_type": "workflow",
  "slug": "consumption-api",
  "name": "Consumption API Pipeline",
  "description": "Expose pipeline or data product events via REST API. Consumers poll or query for events. Ideal for pull-based integrations and ad-hoc querying.",
  "version": "1.0.0",
  "tags": [
    "consumption",
    "api",
    "rest",
    "query",
    "pull"
  ],
  "use_cases": [
    "REST API consumption",
    "Poll-based integration",
    "Ad-hoc querying"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "Consumption pipeline exposing events via REST API",
    "workflow_type": "consumption",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": {
      "schedule": null,
      "batch_size": 100
    },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": {
      "template_slug": "consumption-api",
      "workflow_type": "consumption",
      "delivery_method": "api"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  },
  "dependencies": {},
  "placeholders": {
    "{{WORKFLOW_ID}}": "Generated UUID for the pipeline",
    "{{ORGANIZATION_ID}}": "Organization that owns this pipeline",
    "{{PROJECT_ID}}": "Project containing this pipeline",
    "{{DOMAIN_ID}}": "Domain this pipeline belongs to",
    "{{WORKFLOW_NAME}}": "User-provided name for the pipeline",
    "{{TEMPLATE_ID}}": "Stable template ID from catalog",
    "{{CREATED_AT}}": "ISO 8601 timestamp",
    "{{UPDATED_AT}}": "ISO 8601 timestamp"
  }
}
