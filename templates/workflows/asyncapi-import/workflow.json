{
  "$schema": "https://loxtep.io/schemas/template/workflow.json",
  "template_type": "asyncapi",
  "slug": "asyncapi-import",
  "name": "Import from AsyncAPI",
  "description": "Paste an AsyncAPI 2.x or 3.x YAML spec and create data products from its channels. You will choose project, template, and which channels to import in the next step.",
  "version": "1.0.0",
  "tags": [
    "asyncapi",
    "import",
    "yaml",
    "events",
    "message-driven"
  ],
  "use_cases": [
    "Event API ingestion",
    "Message-driven integrations",
    "AsyncAPI spec adoption"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "AsyncAPI import workflow",
    "template_id": "{{TEMPLATE_ID}}",
    "workflow_type": "ingestion",
    "configuration": {
      "schedule": null,
      "batch_size": null
    },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": {
      "template_slug": "asyncapi-import",
      "ingestion_type": "asyncapi"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  },
  "dependencies": {},
  "placeholders": {
    "{{WORKFLOW_ID}}": "Generated UUID for the workflow",
    "{{ORGANIZATION_ID}}": "Organization that owns this workflow",
    "{{PROJECT_ID}}": "Project containing this workflow",
    "{{DOMAIN_ID}}": "Domain this workflow belongs to",
    "{{WORKFLOW_NAME}}": "User-provided name for the workflow",
    "{{TEMPLATE_ID}}": "Stable template ID from catalog",
    "{{CREATED_AT}}": "ISO 8601 timestamp",
    "{{UPDATED_AT}}": "ISO 8601 timestamp"
  }
}
