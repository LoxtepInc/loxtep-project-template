{
  "$schema": "https://loxtep.io/schemas/template/workflow.json",
  "template_type": "workflow",
  "slug": "consumption-webhook",
  "name": "Consumption Webhook Workflow",
  "description": "Expose workflow or data product events via HTTP webhook delivery. Consumers register a URL and receive events in real time. Ideal for notifications and third-party integrations.",
  "version": "1.0.0",
  "tags": [
    "consumption",
    "webhook",
    "http",
    "real-time",
    "delivery"
  ],
  "use_cases": [
    "Webhook delivery to subscribers",
    "Real-time event push",
    "Third-party integration"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "Consumption workflow delivering events via webhook",
    "workflow_type": "consumption",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": {
      "schedule": null,
      "batch_size": 100
    },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": {
      "template_slug": "consumption-webhook",
      "workflow_type": "consumption",
      "delivery_method": "webhook"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  },
  "dependencies": {},
  "placeholders": {
    "{{WORKFLOW_ID}}": "Generated UUID for the workflow",
    "{{ORGANIZATION_ID}}": "Organization that owns this workflow",
    "{{PROJECT_ID}}": "Project containing this workflow",
    "{{DOMAIN_ID}}": "Domain this workflow belongs to",
    "{{WORKFLOW_NAME}}": "User-provided name for the workflow",
    "{{TEMPLATE_ID}}": "Stable template ID from catalog",
    "{{OWNER_USER_ID}}": "User ID for data product owner and ODPS dataHolder.dataProductOwner",
    "{{CREATED_AT}}": "ISO 8601 timestamp",
    "{{UPDATED_AT}}": "ISO 8601 timestamp"
  }
}
