{
  "key": "file_transfer_preview_file",
  "name": "Preview file contents",
  "description": "Read the first N rows of a remote file. Auto-detects format from extension and content when parserConfig is omitted. Streams only the bytes needed.",
  "version": "1.0.0",
  "type": "action",
  "annotations": {
    "runtime_kind": "platform_native",
    "provider_slug": "file-transfer",
    "operation": "preview_file",
    "read_only": true,
    "destructive": false
  },
  "input_schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "path": {
        "type": "string",
        "description": "File path to preview (relative to transport root)"
      },
      "rows": {
        "type": "integer",
        "minimum": 1,
        "maximum": 1000,
        "description": "Number of data rows to return (default: 10)"
      },
      "parser_config": {
        "type": "object",
        "description": "Optional parser configuration override (delimiter, hasHeader, encoding, etc.)"
      }
    },
    "required": ["path"]
  },
  "execution": {
    "protocol": "platform_native",
    "provider": "file-transfer",
    "operation": "preview_file"
  }
}
