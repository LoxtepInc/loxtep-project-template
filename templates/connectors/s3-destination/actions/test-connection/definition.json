{
  "key": "s3_destination_test_connection",
  "name": "Test S3 connection",
  "description": "HeadBucket / list access check against configured bucket and credentials. Implemented by platform S3DestinationConnector.testConnection (not catalog HTTP).",
  "version": "1.0.0",
  "type": "action",
  "annotations": {
    "runtime_kind": "platform_native",
    "provider_slug": "s3-destination",
    "operation": "test_connection",
    "read_only": true,
    "destructive": false
  },
  "input_schema": {
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "type": "object",
    "additionalProperties": false,
    "properties": {},
    "description": "Uses connector instance config only; no extra inputs."
  },
  "execution": {
    "protocol": "platform_native",
    "provider": "s3-destination",
    "operation": "test_connection"
  }
}
