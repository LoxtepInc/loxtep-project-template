# .loxtep/skills/manufacturing-default.yaml
# Default skill for the "IoT Sensor Monitoring" template.
# Conforms to https://loxtep.io/schemas/skill-package-v1.json
# Scoped to ONLY this template's resources; read-only by default.
# Any resource type or operation not listed below is DENIED (fail-closed).
name: manufacturing-default
description: Read-only access scoped to the IoT Sensor Monitoring template resources
scope:
  data_products: ["sensor-readings-sink"]
  connectors: ["sensor-readings-webhook"]
  workflows: ["sensor-readings-ingestion"]
  domains: []
  queues: []
permissions:
  data_products: [read]
  connectors: [read]
  workflows: [read]
