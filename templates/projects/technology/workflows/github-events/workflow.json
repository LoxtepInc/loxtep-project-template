{
  "$schema": "https://loxtep.io/schemas/workflow-package-v1.json",
  "template_type": "workflow",
  "slug": "github-events-ingestion",
  "name": "GitHub Events Poller Ingestion",
  "description": "Ingest public GitHub events from GH Archive via API polling into the GitHub Activity data product.",
  "version": "1.0.0",
  "tags": [
    "poller",
    "github",
    "ingestion",
    "technology"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "API poller ingestion flow for GitHub events from GH Archive",
    "workflow_type": "ingestion",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": {
      "schedule": {
        "type": "interval",
        "value": 3600
      },
      "batch_size": 100
    },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": {
      "template_slug": "github-events-ingestion",
      "ingestion_type": "poller"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  }
}
