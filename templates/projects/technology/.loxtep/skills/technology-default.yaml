# .loxtep/skills/technology-default.yaml
# Default skill for the "GitHub Activity Tracker" template.
# Conforms to https://loxtep.io/schemas/skill-package-v1.json
# Scoped to ONLY this template's resources; read-only by default.
# Any resource type or operation not listed below is DENIED (fail-closed).
name: technology-default
description: Read-only access scoped to the GitHub Activity Tracker template resources
scope:
  data_products: ["github-activity-sink"]
  connectors: ["github-events-poller"]
  workflows: ["github-events-ingestion"]
  domains: []
  queues: []
permissions:
  data_products: [read]
  connectors: [read]
  workflows: [read]
