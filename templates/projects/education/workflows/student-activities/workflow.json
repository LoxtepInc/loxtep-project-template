{
  "template_type": "workflow",
  "slug": "student-activities-ingestion",
  "name": "Student Activities Webhook Ingestion",
  "description": "Ingest student activity events via webhook into the Student Activities data product.",
  "version": "1.0.0",
  "tags": [
    "webhook",
    "education",
    "ingestion",
    "student-engagement"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "Webhook ingestion flow for student activity events",
    "workflow_type": "ingestion",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": {
      "schedule": null,
      "batch_size": 100
    },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": {
      "template_slug": "student-activities-ingestion",
      "ingestion_type": "webhook"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  }
}
