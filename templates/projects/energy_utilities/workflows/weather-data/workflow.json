{
  "template_type": "workflow",
  "slug": "weather-data-ingestion",
  "name": "Weather Data Poller Ingestion",
  "description": "Ingest weather conditions from OpenWeatherMap API via polling into the Weather Conditions data product.",
  "version": "1.0.0",
  "produces_kind": "source",
  "tags": [
    "poller",
    "energy_utilities",
    "ingestion",
    "weather"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "API poller ingestion flow for weather conditions from OpenWeatherMap",
    "workflow_type": "ingestion",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": {
      "schedule": {
        "type": "interval",
        "value": 1800
      },
      "batch_size": 100
    },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": {
      "template_slug": "weather-data-ingestion",
      "ingestion_type": "poller"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  }
}
