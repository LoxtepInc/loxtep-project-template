# .loxtep/skills/retail-ecommerce-default.yaml
# Default skill for the "Full-Mesh Ecommerce Demo" template.
# Conforms to https://loxtep.io/schemas/skill-package-v1.json
# Scoped to ONLY this template's resources; read-only by default.
# Any resource type or operation not listed below is DENIED (fail-closed).
name: retail-ecommerce-default
description: Read-only access scoped to the Full-Mesh Ecommerce Demo template resources
scope:
  data_products: ["enriched-orders-sink","raw-orders-sink","raw-orders-source"]
  connectors: ["orders-webhook"]
  workflows: ["orders-enrichment","orders-ingestion"]
  domains: []
  queues: []
permissions:
  data_products: [read]
  connectors: [read]
  workflows: [read]
