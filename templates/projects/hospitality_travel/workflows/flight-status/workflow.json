{
  "template_type": "workflow",
  "slug": "flight-status-ingestion",
  "name": "Flight Status Preloaded Ingestion",
  "description": "Ingest flight status data from preloaded dataset into the Flight Status data product.",
  "version": "1.0.0",
  "produces_kind": "source",
  "tags": [
    "preloaded",
    "hospitality_travel",
    "ingestion",
    "flights"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "Preloaded ingestion flow for flight status data",
    "workflow_type": "ingestion",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": {
      "schedule": null,
      "batch_size": 100
    },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": {
      "template_slug": "flight-status-ingestion",
      "ingestion_type": "preloaded"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  }
}
