{
  "template_type": "trigger",
  "slug": "flight-status-preloaded-connection",
  "name": "Flight Status Preloaded Connection",
  "description": "Preloaded data connection for flight status dataset.",
  "version": "1.0.0",
  "tags": [
    "preloaded",
    "ingestion",
    "hospitality_travel",
    "flights"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "upstream_entity_id": null,
    "upstream_entity_type": null,
    "key": "flight-status-preloaded",
    "name": "Flight Status Preloaded",
    "type": "preloaded",
    "status": "active",
    "configuration": {
      "dataset_key": "demo-data/travel/flights.jsonl",
      "queue_name": "{org}-demo-flight-status",
      "description": "Flight delays dataset pre-loaded into queue"
    },
    "metadata": {},
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}",
    "trigger_id": "{{CONNECTION_ID}}"
  },
  "placeholders": {
    "{{CONNECTION_ID}}": "Generated UUID for the connection",
    "{{WORKFLOW_ID}}": "Workflow this connection belongs to",
    "{{ORGANIZATION_ID}}": "Organization that owns this connection",
    "{{PROJECT_ID}}": "Project containing this connection",
    "{{CREATED_AT}}": "ISO 8601 timestamp",
    "{{UPDATED_AT}}": "ISO 8601 timestamp"
  }
}
