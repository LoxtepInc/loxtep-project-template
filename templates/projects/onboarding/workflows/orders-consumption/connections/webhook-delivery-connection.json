{
  "$schema": "https://loxtep.io/schemas/trigger-package-v1.json",
  "template_type": "trigger",
  "slug": "webhook-delivery-connection",
  "name": "Webhook Delivery Connection",
  "description": "Outbound webhook connection for delivering enriched order events to a partner system.",
  "version": "1.0.0",
  "tags": [
    "webhook",
    "consumption",
    "delivery"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "key": "webhook-delivery",
    "name": "Webhook delivery",
    "description": "Deliver enriched order events to partner webhook URLs",
    "type": "webhook",
    "status": "active",
    "upstream_entity_id": "{{DATA_PRODUCT_ID}}",
    "upstream_entity_type": "data-products",
    "configuration": {},
    "metadata": {
      "template_slug": "orders-consumption"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}",
    "trigger_id": "{{CONNECTION_ID}}"
  },
  "placeholders": {
    "{{CONNECTION_ID}}": "Generated UUID for the connection",
    "{{WORKFLOW_ID}}": "Workflow this connection belongs to",
    "{{ORGANIZATION_ID}}": "Organization that owns this connection",
    "{{PROJECT_ID}}": "Project containing this connection",
    "{{DATA_PRODUCT_ID}}": "Upstream data product ID (consumption flows)",
    "{{CREATED_AT}}": "ISO 8601 timestamp",
    "{{UPDATED_AT}}": "ISO 8601 timestamp"
  }
}
