{
  "$schema": "https://loxtep.io/schemas/template/workflow.json",
  "template_type": "workflow",
  "slug": "orders-consumption",
  "name": "Orders to Partner Webhook",
  "description": "Consume enriched orders and send to partner system via webhook.",
  "version": "1.0.0",
  "tags": ["consumption", "webhook", "orders", "e-commerce"],
  "use_cases": ["Webhook delivery to subscribers", "Real-time event push"],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "Consumption flow delivering enriched orders via webhook",
    "workflow_type": "consumption",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": { "schedule": null, "batch_size": 100 },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": {
      "template_slug": "orders-consumption",
      "workflow_type": "consumption",
      "delivery_method": "webhook"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  }
}
