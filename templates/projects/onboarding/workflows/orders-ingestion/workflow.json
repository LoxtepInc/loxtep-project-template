{
  "$schema": "https://loxtep.io/schemas/template/workflow.json",
  "template_type": "workflow",
  "slug": "orders-ingestion",
  "name": "Orders Webhook Ingestion",
  "description": "Ingest orders from external store via webhook into the Raw Orders data product.",
  "version": "1.0.0",
  "tags": ["webhook", "orders", "ingestion", "e-commerce"],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "Webhook ingestion flow for order events",
    "workflow_type": "ingestion",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": { "schedule": null, "batch_size": 100 },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": { "template_slug": "orders-ingestion", "ingestion_type": "webhook" },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  }
}
