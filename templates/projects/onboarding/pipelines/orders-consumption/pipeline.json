{
  "$schema": "https://loxtep.io/schemas/template/pipeline.json",
  "template_type": "pipeline",
  "slug": "orders-consumption",
  "name": "Orders to Partner Webhook",
  "description": "Consume enriched orders and send to partner system via webhook.",
  "version": "1.0.0",
  "tags": ["consumption", "webhook", "orders", "e-commerce"],
  "use_cases": ["Webhook delivery to subscribers", "Real-time event push"],
  "entity": {
    "pipeline_id": "{{PIPELINE_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{PIPELINE_NAME}}",
    "description": "Consumption pipeline delivering enriched orders via webhook",
    "pipeline_type": "consumption",
    "upstream_flow_id": "{{UPSTREAM_FLOW_ID}}",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": { "schedule": null, "batch_size": 100 },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": { "template_slug": "orders-consumption", "pipeline_type": "consumption", "delivery_method": "webhook" },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  }
}
