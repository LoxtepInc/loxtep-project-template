{
  "$schema": "https://loxtep.io/schemas/workflow-package-v1.json",
  "template_type": "workflow",
  "slug": "crypto-prices-ingestion",
  "name": "Crypto Prices Poller Ingestion",
  "description": "Ingest real-time cryptocurrency prices from CoinGecko API via polling into the Crypto Prices data product.",
  "version": "1.0.0",
  "produces_kind": "source",
  "tags": [
    "poller",
    "cryptocurrency",
    "ingestion",
    "financial_services"
  ],
  "entity": {
    "workflow_id": "{{WORKFLOW_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "project_id": "{{PROJECT_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{WORKFLOW_NAME}}",
    "description": "API poller ingestion flow for cryptocurrency prices from CoinGecko",
    "workflow_type": "ingestion",
    "template_id": "{{TEMPLATE_ID}}",
    "configuration": {
      "schedule": {
        "type": "interval",
        "value": 300
      },
      "batch_size": 100
    },
    "deployment": null,
    "status": "inactive",
    "metrics": {},
    "metadata": {
      "template_slug": "crypto-prices-ingestion",
      "ingestion_type": "poller"
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}"
  }
}
