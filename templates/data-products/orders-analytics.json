{
  "template_type": "data_product",
  "slug": "orders-analytics",
  "name": "Orders Analytics Data Product",
  "description": "Order and transaction data product for sales analytics, reporting, and downstream consumption. Includes validated order events with line items and totals.",
  "version": "1.0.0",
  "tags": ["orders", "sales", "analytics", "e-commerce", "transactions"],
  "use_cases": ["Sales reporting", "Revenue analytics", "Order lifecycle tracking"],
  "entity": {
    "data_product_id": "{{DATA_PRODUCT_ID}}",
    "organization_id": "{{ORGANIZATION_ID}}",
    "workflow_id": "{{WORKFLOW_ID}}",
    "domain_id": "{{DOMAIN_ID}}",
    "name": "{{DATA_PRODUCT_NAME}}",
    "description": "Order and transaction data for sales analytics and reporting.",
    "owner": { "user_id": "{{USER_ID}}" },
    "status": "draft",
    "governance": {
      "classification": "internal",
      "pii_fields": ["customer_email", "shipping_address"],
      "compliance_requirements": [],
      "tags": ["orders", "sales", "analytics"]
    },
    "metadata": {
      "business_glossary": {},
      "refresh_frequency": "real-time",
      "expected_latency_ms": 5000
    },
    "created_at": "{{CREATED_AT}}",
    "updated_at": "{{UPDATED_AT}}",
    "odps_document": {
      "product": {
        "details": {
          "en": {
            "name": "{{DATA_PRODUCT_NAME}}",
            "description": "Order and transaction data for sales analytics and reporting.",
            "status": "draft",
            "tags": ["orders", "sales", "analytics"],
            "type": "orders-analytics",
            "governanceProfile": "internal"
          }
        },
        "dataHolder": {
          "businessDomain": "{{DOMAIN_ID}}",
          "dataProductOwner": "{{USER_ID}}"
        },
        "custom": {
          "loxtep": {
            "organization_id": "{{ORGANIZATION_ID}}",
            "domain_id": "{{DOMAIN_ID}}",
            "project_id": "{{PROJECT_ID}}",
            "workflow_id": "{{WORKFLOW_ID}}"
          }
        }
      }
    }
  },
  "placeholders": {
    "{{DATA_PRODUCT_ID}}": "Generated UUID for the data product",
    "{{ORGANIZATION_ID}}": "Organization that owns this data product",
    "{{WORKFLOW_ID}}": "Workflow (flow) that produces this data product in the graph",
    "{{PROJECT_ID}}": "Project containing the workflow",
    "{{DOMAIN_ID}}": "Domain this data product belongs to",
    "{{DATA_PRODUCT_NAME}}": "User-provided name for the data product",
    "{{USER_ID}}": "Owner user ID",
    "{{CREATED_AT}}": "ISO 8601 timestamp",
    "{{UPDATED_AT}}": "ISO 8601 timestamp"
  }
}
